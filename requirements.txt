# Dependencias directas del backend Django.
# BORRADOR: las versiones marcadas "sin fijar" hay que fijarlas con el `pip freeze`
# del servidor (el que de verdad ejecuta la app) antes de usar esto en un deploy.
Django==5.0.3
django-cors-headers==4.3.1
djangorestframework==3.15.1
django-filter          # sin fijar
pillow==10.2.0
psycopg2-binary        # sin fijar (o psycopg 3, segun lo que use el servidor)
gunicorn               # sin fijar
boto3                  # sin fijar
django-storages        # sin fijar
python-dotenv          # sin fijar
